# Server-side Environment Variables
# NEVER expose these in frontend code

# Privy App Secret - Used for server-side verification only
PRIVY_APP_SECRET=your-privy-app-secret-here

# Firebase Admin SDK (if needed for server-side operations)
FIREBASE_SERVICE_ACCOUNT_KEY=path/to/serviceAccountKey.json

# Database URLs
DATABASE_URL=your-database-url

# API Keys for server-side services
ORACLE_API_KEY=your-oracle-api-key